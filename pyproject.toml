[project]
name = "finance-dashboard"
version = "0.1.0"
description = "Finance dashboard with Django and Plotly"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0",
    "pandas>=3.0",
    "plotly>=6.0",
    "matplotlib>=3.10",
]
